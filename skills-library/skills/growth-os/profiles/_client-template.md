# Profile — <Brand> (template)

Copy per client → `profiles/<client>.md`. Fill it from the 22 audit BEFORE
producing anything. A profile with empty fields is not a profile — do the
audit first.

## Identity
- Person/brand: · Niche (one): · One outcome sold:
- Focus sentence (Gate 3): `I help [who] who feel [tension] by [mechanism] —
  and I do not help [who not].`
- Audience: age ≈ the creator's own, situation, top 3 pains, top objection.
- DM keyword CTA: · Proof inventory (real only):

## Voice
- Dialect/register: · 3 signature phrases: · Banned words:

## Look
- Palette pulled from THEIR real footage/environment (3–4 hexes + shades per
  situation — 22's method, not invented colors):
- Type: ONE family, 2 weights. AR+EN pairing with the same skeleton (21):
- Canvas + top-bar/footer pattern:
- `vibe.json` six levers (stock / light / grade / lens / camera / composition)
  + signature + antiStyle:

## Refs
- Face/pose ref images: · face-lock? · what the model must never do:

## Plant
- Render project: scaffold with carousel-forge `init.mjs` → `<path>`
- Video: recipe A/B/C mix this brand uses (20):
- Publishing: manual until the client hands Meta assets (`META_PAGE_TOKEN` +
  IG business id) — then clone the `scheduler/` pattern into their repo (24).

## KPI math (22's backwards chain)
- Goal $/mo → clients/mo → calls → engaged viewers (≈0.5% VSL conversion,
  500–1,000 engaged viewers per client) → posting volume.

## Cadence
- Weekly plan (per 09/15): · Slots: · Review day:
