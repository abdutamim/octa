# Task: spec 004 — Research engine (100 sources, five languages, research browser)

Read `docs/02-RESEARCH-ENGINE.md` in full, `docs/01-RUNTIME-CONTRACT.md` §5, `docs/07-DECISIONS.md` D3/D4/D5/D17, and `docs/06-SPECS.md` section 004.

Build:
1. `electron/core/octa/research.ts` — `runResearch(task, opts)`: prompt assembly = task + the full protocol §2 + language rule (always ar, en, fr, de, ru + extras) + ledger schema §3 + budget (45 min default); runs the scout via spec 001's `codex-exec` runner with `--output-schema evidence.schema.json` (write that schema: `{frame, language_plan[], sources[], themes[], contradictions[], unknowns[], report_path}`); saturation logic and budget are instructions to the model, the gates are code.
2. Gates in code (§7): distinct URLs ≥100, domains ≥40 (max 8 per domain count), per-language ≥10 where material exists with "no material" entries carrying their 15 tried queries, every claim's source number exists, report language matches the conversation language (script-ratio check). Fail → `failed: depth` with a partial report kept. Reuse spec 012's `sources.ts` validators if present; otherwise implement here and note it.
3. `octa-tools` MCP server (`electron/core/octa/mcp-server.ts`, stdio, `@modelcontextprotocol/sdk`): tools `search(query, lang, engine=google|bing|ddg)` (parses result pages in the research browser), `open(url) → {markdown, screenshot_path}`, `scroll_collect(url, n)`, `youtube_transcript(url)`, `reddit_search(query)` (public JSON), `brave_search(query, lang)` when a key exists. Registered for both CLIs via `--mcp-config` (Claude) and Codex `mcp_servers` config passed with `-c`.
4. Research browser (`electron/core/octa/browser.ts`): persistent Playwright Chromium context at `<octaHome>/browser/research` (`PLAYWRIGHT_BROWSERS_PATH` under octaHome); `loginFlow(site)` opens a headed window and waits until the owner confirms in-app; read-only tool surface (no click on post/like/comment/send controls: block by role/text heuristics + never call `page.click` on buttons inside composer areas); pacing ≤1 page / 4 s per domain, ≤200 pages per social site per job; on captcha or challenge → emit `browser:challenge` for the owner and back off that site 24 h (persist in settings).
5. Persona and competitor job presets (§4, §5) as prompt templates under `electron/core/octa/prompts/research-*.md`.
6. IPC `research:start|status|challenge:resolved|login:start|login:done`. Minimal UI: a "Research browser" section in Settings (login buttons per site, status) and a ledger viewer component (`SourcesTable.tsx`) used by the Jobs page.
7. Tests: gate logic with fixtures (pass, depth fail, language fail, bad citation), ledger parser, MCP tool schemas, pacing limiter, composer-blocking heuristic on a fixture DOM.
8. One real run (budget 15 min for the test, so expect `failed: depth` with a partial report): "dream buyer persona لعيادة أسنان في القاهرة". Paste the language plan and the source count into the report.
